*.db
*.db-shm
*.db-wal
*.csv
*.jsonl

# local env overrides (see .env.example)
.env

# frontend
frontend/web/node_modules/
frontend/web/dist/

# backend
backend/opencharge-api
backend/opencharge-ingest

# per-source failed-URL files left by ingestion runs (replayed with
# `opencharge-ingest -retry-failed`)
ingest-failures/
backend/ingest-failures/